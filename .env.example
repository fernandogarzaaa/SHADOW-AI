SHADOW_NODE_HOST=127.0.0.1
SHADOW_NODE_PORT=8787

# --- Privacy / transport ---
SHADOW_PRIVACY_MODE=strict_local
# Require signed, replay-protected requests on all protected routes (production: true).
SHADOW_AUTH_REQUIRED=false
# Draft-then-verify: retry a poorly-grounded frontier answer against full context.
SHADOW_GROUNDING_VERIFY=true
# Per-client request cap (requests/minute). 0 disables. /health and /ready are exempt.
SHADOW_RATE_LIMIT_RPM=120

# --- Persistent encrypted memory ---
# When set, memory persists across restarts using a stable key (below).
SHADOW_MEMORY_DB=./data/shadow_memory.db
# Provide a key directly (urlsafe base64 Fernet key) OR point to a key file.
# If neither is set, a key file is generated at data/keys/memory.key (0600).
SHADOW_MEMORY_KEY=
SHADOW_MEMORY_KEY_FILE=./data/keys/memory.key

# --- Persistent encrypted runtime state (audit, consents, devices) ---
# When set, this state survives restarts; unset = in-memory only.
SHADOW_RUNTIME_DB=./data/shadow_runtime.db
SHADOW_RUNTIME_KEY=
SHADOW_RUNTIME_KEY_FILE=./data/keys/runtime.key

# --- Real action execution (GHOST) ---
# 'local' runs real sandboxed actions (notes, reminders, public web fetch);
# 'mock' simulates them. File actions are confined to the workspace below.
GHOST_RUNTIME_MODE=local
SHADOW_WORKSPACE_DIR=./data/workspace

# --- Model providers (hybrid local + frontier) ---
# Default is fully offline. Pick the active frontier provider for escalation.
SHADOW_CLOUD_ENABLED=false
SHADOW_MODEL_PROVIDER=anthropic          # anthropic | openai | gemini | local_mock
SHADOW_MODEL_NAME=claude-sonnet-4-6
SHADOW_MODEL_ENDPOINT=

# Frontier credentials. Connect via the dashboard (encrypted at rest) OR set here.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
# Provider credentials are stored encrypted; override the location if you like.
SHADOW_PROVIDERS_FILE=./data/keys/providers.enc
SHADOW_PROVIDERS_KEY_FILE=./data/keys/providers.key

# Google OAuth (only legitimate subscription-free OAuth path: bills your GCP/Vertex).
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=

# --- Context runtime ---
AXIOM_RUNTIME_MODE=deterministic
