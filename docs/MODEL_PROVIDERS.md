# Model Providers
Providers: LocalMockModelProvider, OpenAI-compatible, OpenRouter-compatible, and LocalModel placeholder. Cloud providers are blocked unless explicit request approval and an active non-local ConsentGrant exist. Context is redacted, compressed, skeletonized, token-estimated, and audited before any cloud use.
