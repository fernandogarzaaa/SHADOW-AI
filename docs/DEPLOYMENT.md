# Deployment
Run locally with Python, Docker, or compose. Use `.env.example` for host, runtime DB, memory DB, and provider configuration. Do not expose Shadow Node to untrusted networks without TLS/authenticated device sessions.
