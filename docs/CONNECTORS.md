# Connectors
Mock/local connectors exist for Gmail, Calendar, Files, Notes/manual text, and Desktop Node task execution. They declare capabilities, consent scopes, and future OAuth scopes. Real OAuth is intentionally not faked; future providers must bind consent grants to selected scopes and retention.
