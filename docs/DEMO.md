# Demo
Terminal 1: `make run`. Terminal 2: `make demo`. The script pairs a demo device, ingests `examples/demo_memory.md`, asks a question, creates/approves an action, executes via Ghost mock, and prints audit events.
